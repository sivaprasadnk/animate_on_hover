name: animate_on_hover
description: Add animation(s) to your widgets on web when hovered, using this package.
version: 1.0.2
homepage: https://github.com/sivaprasadnk/animate_on_hover

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
